export enum ErrorCode {
  NO_PERMISSION = 'permission.none',
  SESSION_EMPTY = 'session.empty',

  // user
  USER_EMPTY = 'user.empty',

  // profile
  PROFILE_EMPTY = 'profile.empty',
  PROFILE_BIRTHDAY_UNEDITABLE = 'profile.birthday.uneditable',
  PROFILE_NATAL_FETCH_ERROR = 'profile.natal.fetch_error',
}
