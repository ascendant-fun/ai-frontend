/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  //remove this if SSR
  // images: {
  //   unoptimized: true,
  // },
};

module.exports = nextConfig;
